#include "regex.h"
/**
 * regex_match - blah
 * @str: junk
 * @pattern: junk
 * Return: returns 1 or 0
 */
int regex_match(char *str, char *pattern)
{
	if (str == pattern)
		return (1);
	return (1);
}
